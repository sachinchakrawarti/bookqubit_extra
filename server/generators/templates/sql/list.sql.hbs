(
echo -- List all {{moduleName}}s with pagination
echo SELECT *
echo FROM {{kebabCase moduleName}}
echo WHERE deleted_at IS NULL
echo ORDER BY created_at DESC
echo LIMIT ? OFFSET ?;
) > plop-templates\sql\list.sql.hbs